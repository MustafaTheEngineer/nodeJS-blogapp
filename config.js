/* const config = {
  db: {
    host: "localhost",
    user: "root",
    password: "Muhendis01.",
    database: "blogapp",
  },
}; */

const config = {
  db: {
    host: "localhost",
    user: "root",
    password: "123",
    database: "blogdb",
  },
};

module.exports = config;
